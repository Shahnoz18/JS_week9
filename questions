1.Какие бывают варианты объявления переменных?
Ответ:  var, const, let
2. Как можно вывести сообщение "Hello World" с помощью JavaScript? (выберите один верный вариант)
    1. msgBox('Hello World');
    2. console.log('Hello World');
    3. msg('Hello World');
    4. alertBox('Hello World');
Ответ:  2
3. Что выведет этот скрипт?
Ответ:  alert( `hello ${1}` ); // hello 1!
        alert( `hello ${«name»}` );  // hello name!
        alert( `hello ${name}` ); // hello Ilya!
4. Какое значение будет в z?
    let x = 5;         
    let y = 2;        
    let z = x + y;
    console.log(z);
Ответ:  7
5. Какими тремя способами можно подключить JavaScript код?
Ответ:  внутри тегов <script></script перед закрывающим </body>;
        в строке, в конкретном теге;
        подключить через внешний файл: <script src="путь_к_файлу_скрипта/script.js"></script>;
6. Есть ли какая-то разница между записями typeof str и typeof(str)?
Ответ:  Нет никакой разницв, можно использовать как со скобками, так и без
7. Создайте переменную z, к ней присвойте переменные x+y и выведите результат в консоль. Допишите необходимый код вместо пробелов.
    const x = 5;
    const y = 10; 
    _ _ = x + y;
    console.log(_);
Ответ:  const z = x + y;
        console.log(z);
8. Какой результат будет у выражений ниже?
Ответ:  "" + 1 + 0
    "" - 1 + 0
    true + false
    6 / "3"
    "2" * "3"
    4 + 5 + "px"
    "$" + 4 + 5
    "4" - 2
    "4px" - 2
    7 / 0
    "  -9  " + 5
    "  -9  " - 5
    null + 1
    undefined + 1
    " \t \n" - 2 
9. Что будет в x?
    x = 5; 
    x = 2;
    console.log(x);
Ответ:  2
10. Что будет в x?
    x = 5; 
    x -= 2;
    console.log(x);
Ответ: 
11. Как превратить переменную, содержащую строку, в число?
    x = '5'; 
    x = ???; //какой код нужно подставить вместо ???, чтобы следующая строка вывела нам number?
    console.log(typeof(x)); //number
Ответ:  